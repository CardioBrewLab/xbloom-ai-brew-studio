{
  "name": "@xbloom/cloudflare-hosted",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "npm run typecheck && wrangler deploy --dry-run --config wrangler.template.jsonc",
    "test": "node --experimental-strip-types --test test/*.test.ts",
    "typecheck": "tsc --noEmit",
    "types": "wrangler types --config wrangler.template.jsonc"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260810.1",
    "@types/node": "^24.0.10",
    "typescript": "^7.0.2",
    "wrangler": "^4.120.1"
  },
  "allowScripts": {
    "esbuild@0.28.1": true,
    "workerd@1.20260804.1": true
  }
}
